import { createStore } from "vuex";

export default createStore({
  state: {
    "mapView": {
      "settings": {
        "keepCentered": true,
        "showInfoWidget": true
      },
    },
  },
  mutations: {},
  actions: {},
  modules: {}
});